# -*- mode: snippet -*-
# name: compete
# key: compete
# --
use proconio::input;

#[proconio::fastout]
fn main() {
    input! {
        n: usize,
        a: [usize; n],
        b: [usize; n],
        c: [usize; n],
    }
    let mut count = 0;

    println!("{}", count);
}
